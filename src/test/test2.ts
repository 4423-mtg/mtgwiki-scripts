import path from "node:path";
import { fetchOracleCardsBulkData } from "../lib/scryfall.js";

// await downloadOracleCards();

const directory = String.raw`./data`;
console.log(path.parse(directory));
0;
